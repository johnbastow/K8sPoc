{
    "$schema": "http://json-schema.org/draft-07/schema",
    "required": [
        "instance",
        "phase"
    ],
    "properties": {
        "instance": {
            "type": "string"
        },
        "phase": {
            "type": "string",
            "enum": ["dev", "qa", "uat", "prod"]
        }
    }
 }